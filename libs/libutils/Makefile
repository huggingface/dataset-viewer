# environment variables for the commands (docker-compose, poetry)
export TEST_COMPOSE_PROJECT_NAME := libutils
# makefile variables
TEST_DOCKER_COMPOSE := ../../tools/docker-compose-empty.yml

include ../../tools/Python.mk
include ../../tools/Docker.mk
