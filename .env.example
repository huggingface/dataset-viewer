###########################
# make run/watch/test ... #
###########################

# Application hostname
# APP_HOSTNAME="localhost"

# Application port
# APP_PORT=8000

# Assets directory
# ASSETS_DIRECTORY=

# Cache directory (see http://www.grantjenks.com/docs/diskcache/tutorial.html)
# CACHE_DIRECTORY=

# Persist the cache between runs by using the same directory
# CACHE_PERSIST=True

# Maximum size of the cache in bytes
# CACHE_SIZE_LIMIT=10000000000

# Enable private datasets
# DATASETS_ENABLE_PRIVATE=False

# Number of rows extracted from the dataset, if not specified otherwise
# EXTRACT_ROWS_LIMIT=100

# Git reference for the canonical datasets on https://github.com/huggingface/datasets
# DATASETS_REVISION="master"

# Hugging Face token (see https://huggingface.co/settings/token)
# HF_TOKEN=

# Log level
# LOG_LEVEL = "INFO"

# Number of seconds to set in the `max-age` header on data endpoints
# MAX_AGE_LONG_SECONDS=21600

# Number of seconds to set in the `max-age` header on technical endpoints
# MAX_AGE_SHORT_SECONDS=120

# Number of uvicorn workers
# WEB_CONCURRENCY = 1

#########################
# make warm and refresh #
#########################

# Max CPU load (%) - if reached, sleeps until it comes back under the limit
# DEFAULT_MAX_LOAD_PCT = 50

# Max RAM (%) - if reached, exits
# DEFAULT_MAX_VIRTUAL_MEMORY_PCT = 95

# Max SWAP (%) - if reached, exits
# DEFAULT_MAX_SWAP_MEMORY_PCT = 80

# Percentage of datasets to refresh
# DEFAULT_REFRESH_PCT = 1
