apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    {{ include "labels.worker.splitsNext" . | nindent 4 }}
  name: "{{ include "release" . }}-worker-splits-next"
  namespace: {{ .Release.Namespace }}
spec:
  progressDeadlineSeconds: 600
  replicas: {{ .Values.worker.splitsNext.replicas }}
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      {{ include "labels.worker.splitsNext" . | nindent 6 }}
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        {{ include "labels.worker.splitsNext" . | nindent 8 }}
    spec:
      initContainers:
        {{ include "initContainerAssets" . | nindent 8 }}
        {{ include "initContainerCache" . | nindent 8 }}
        {{ include "initContainerNumbaCache" . | nindent 8 }}
      containers:
        {{ include "containerWorkerSplitsNext" . | nindent 8 }}
      nodeSelector:
        {{ toYaml .Values.worker.splitsNext.nodeSelector | nindent 8 }}
      tolerations:
        {{ toYaml .Values.worker.splitsNext.tolerations | nindent 8 }}
      volumes:
        - name: nfs
          nfs:
            server: {{ .Values.storage.nfs.server }}
            path: {{ .Values.storage.nfs.path }}
      securityContext:
        runAsUser: {{ .Values.uid }}
        runAsGroup: {{ .Values.gid }}
        runAsNonRoot: true
