# SPDX-License-Identifier: Apache-2.0
# Copyright 2022 The HuggingFace Authors.

apiVersion: apps/v1
kind: Job
metadata:
  labels:
    {{ include "labels.migration" . | nindent 4 }}
  name: "{{ include "release" . }}-job-migration"
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-1"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  template:
    metadata:
      labels:
        {{ include "labels.migration" . | nindent 8 }}
    spec:
      restartPolicy: Never
      containers:
        {{ include "containerMigration" . | nindent 8 }}
      nodeSelector:
        {{ toYaml .Values.migration.nodeSelector | nindent 8 }}
      tolerations:
        {{ toYaml .Values.migration.tolerations | nindent 8 }}
      securityContext:
        runAsUser: {{ .Values.uid }}
        runAsGroup: {{ .Values.gid }}
        runAsNonRoot: true
  backoffLimit: 0
