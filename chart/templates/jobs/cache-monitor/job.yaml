# SPDX-License-Identifier: Apache-2.0
# Copyright 2022 The HuggingFace Authors.

{{- if .Values.images.jobs.cacheMonitor }}
apiVersion: batch/v1
kind: Job
metadata:
  labels: {{ include "labels.cacheMonitor" . | nindent 4 }}
  name: "{{ include "name" . }}-job-cache-monitor"
  namespace: {{ .Release.Namespace }}
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels: {{ include "labels.cacheMonitor" . | nindent 8 }}
    spec:
      restartPolicy: Never
      {{- include "image.imagePullSecrets" . | nindent 6 }}
      containers: {{ include "containerCacheMonitor" . | nindent 8 }}
      nodeSelector: {{ toYaml .Values.cacheMonitor.nodeSelector | nindent 8 }}
      tolerations: {{ toYaml .Values.cacheMonitor.tolerations | nindent 8 }}
      securityContext: {{ include "securityContext" . | nindent 8 }}
  backoffLimit: 0
{{- end}}